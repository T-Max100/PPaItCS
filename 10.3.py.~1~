from graphics import *
from button import Button
from randon import randint

def main():
    results = drawButtons()
    win_lose = conclusion(results)

def drawButtons():
    win = GraphWin("Three Button Monte", 600, 600)
    win.setCoords(0.0, 0.0, 6.0, 6.0)
    greet(win)
    threeButtons(win)
    return results

def greet(win):
    message = Text(Point(3, 0.3), "Guess the correct door to win!")
    message.draw(window)
    window.getMouse()
    message.undraw()

def threeButtons(win):
    

if __name__ == "__main__":
    main()
